// Binary Search Array v.2
// Recursion with array.splice()

function binarySearch (array, target) {

	// base case to stop recursion
	// if array length = 0, target not in array
	if (array.length === 0) {
		return -1;
	}

	// determine array index midpoint
	let iMid = Math.floor(arr.length / 2);


	if (array[iMid] === target) {
		return iMid
	}



}